export interface CompetitionData {
    name: string;
    deadline: string;
    organiserName: string;
    description: string;
    urlLink: string;
    maxSize: number | null;
    minSize: number | null;
  }
  